export const metadata = {
  title: 'Contact Us | Fenetra Industries',
  description: 'Get in touch with Fenetra Industries for raw material quotations, technical support, and general enquiries.',
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
